[pytest]
testpaths = tests
addopts = -q -m "not integration and not slow"
markers =
    integration: tests that exercise external systems or end-to-end paths.
    slow: tests that are intentionally slower than core unit checks.
